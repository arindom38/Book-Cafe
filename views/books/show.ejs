<h2 class="page-header">
    <%= book.title %>
</h2>
<img height="150" width="100" src="<%= book.coverImagePath %> " alt="<%= book.title %> ">
<div>Author: <%= book.author.name %>
</div>
<div>Publish Date: <%= book.publishDate.toDateString() %>
</div>
<div>Page Count: <%= book.pageCount %>
</div>
<div>Description: <%= book.description %>
</div>
<div class="btn-row">
    <a class="btn btn-primary" href="/books/<%= book.id %>/edit">Edit</a>
    <%- include('../partials/_deleteField',{url: `/books/${book.id}`}) %>
    <a class="btn btn-primary" href="/authors/<%= book.author.id %>">View Author</a>
</div>
